# environment-manager-server

API em Fastify + TypeScript.

## Scripts

```bash
npm run dev     # desenvolvimento com hot reload (tsx)
npm run build   # compila para dist/
npm start       # roda dist/index.js
```

Servidor em `http://localhost:3000` — `GET /` retorna `{ "message": "Hello World" }`.
